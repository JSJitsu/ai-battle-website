<div class="container">
  <div class="row">
    <div class="col-lg-12 text-center">
      <h2><%= get('githubHandle') %></h2>
        <hr class="star-primary">
    </div>
  </div> 
  <div class="row">
    <ul class="nav nav-tabs col-lg-12 options-tabs userViewTabs" role="tablist">
      <li class="active settings"><a href="#">Settings</a></li>
      <li id="tab" class="recentStats"><a href="#">Recent Stats</a></li>
      <li class="lifetimeStats"><a href="#">Lifetime Stats</a></li>
      <li class="averageStats"><a href="#">Average Stats</a></li>
    </ul>
  </div>
    <div class="settingsView">
      <p>
        You are currently entered in the JavaScript Battle.  We are using the following GitHub respository from your account.  Feel free to update this repository, but keep in mind we will import your latest code at ~3:00 AM (PST) every day.  
      </p>
      <form class="form-inline repoInput text-center" role="form">
        <div class="form-group">
          <label class="control-label" for="inputRepo">GitHub Repository</label>
          <input type="text" class="form-control" id="inputRepo" value="<%= get('codeRepo') %>">
          <span class="glyphicon glyphicon-ok form-control-feedback"></span>  
        </div>
        <div class="form-group">
          <button type="submit" class="btn btn-default submitRepo">Save</button>
        </div>
      </form>
    </div>
  </div> 
    <div class="text-center">
      <button type="button" class="btn btn-default btn-lg logoutButton">
        <a class="glyphicon glyphicon-log-out" href="/logout">Log-out</a> 
      </button>
    </div>
</div>